#logging.level.org.springframework.web=DEBUG

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/ajax-demo
        username: root
        password: test1234
        driver-class-name: com.mysql.jdbc.Driver

    jpa:
      hibernate:
          ddl-auto: update
      properties:
          hibernate:
              show_sql: true
              format_sql: true
              dialect: org.hibernate.dialect.MySQL5Dialect